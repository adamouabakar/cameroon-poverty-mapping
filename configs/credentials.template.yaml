# Template credentials — copier vers credentials.local.yaml (gitignored)
# Ne jamais committer de secrets.

gee:
  project_id: "votre-projet-gee"
  # Après installation : earthengine authenticate
  # Vérifier : earthengine authenticate --force

dhs:
  # Placer les fichiers dans data/raw/dhs/
  # CMGE71FL/ (shapefile GPS) + CMHR71DT/ (household recode)
  access: "https://dhsprogram.com/data/dataset/Cameroon_Standard-DHS_2018.cfm"

ins:
  # Référence versionnée (pas de secret)
  reference_csv: "data/reference/ins/ecam4_regional_indicators.csv"
  # PDF optionnel (gitignored)
  pdf_url: "https://ins-cameroun.cm/wp-content/uploads/2025/06/Pauvrete_et_evolution_du_pouvoir_d_achat_des_menages_ECAM4.pdf"
  contact: "infos@ins-cameroun.cm"

github:
  repo: "adamouabakar/cameroon-poverty-mapping"
  pages_url: "https://adamouabakar.github.io/cameroon-poverty-mapping/"